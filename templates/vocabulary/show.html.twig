{% extends 'base.html.twig' %}

{% block title %}Vocabulary{% endblock %}

{% block navbar %}
    {{ include('main/_navbar.html.twig') }}
{% endblock %}

{% block container %}

    <div class="row">
        <div class="h1">Vocabulary</div>
    </div>

    <div class="row">
        <div class="col-sm-8">
            <table class="table table-hover">
                <tbody>
                <tr scope="row">
                    <th>Id</th>
                    <td>{{ vocabulary.id }}</td>
                </tr>
                <tr scope="row">
                    <th>Word</th>
                    <td>{{ vocabulary.word }}</td>
                </tr>
                </tbody>
            </table>
        </div>
    </div>

    <a class="btn btn-primary" href="{{ path('vocabulary_index') }}">back to list</a>
    <a class="btn btn-primary" href="{{ path('vocabulary_edit', {'id': vocabulary.id}) }}">edit</a>

    {{ include('vocabulary/_delete_form.html.twig') }}

{% endblock %}
