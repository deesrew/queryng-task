{% extends 'base.html.twig' %}

{% block title %}Task{% endblock %}

{% block container %}
    <h1>Task</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ task.id }}</td>
            </tr>
            <tr>
                <th>Field1</th>
                <td>{{ task.field1 }}</td>
            </tr>
            <tr>
                <th>Field2</th>
                <td>{{ task.field2 }}</td>
            </tr>
            <tr>
                <th>Date_created</th>
                <td>{{ task.dateCreated ? task.dateCreated|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('task_index') }}">back to list</a>

    <a href="{{ path('task_edit', {'id': task.id}) }}">edit</a>

    {{ include('task/_delete_form.html.twig') }}
{% endblock %}
