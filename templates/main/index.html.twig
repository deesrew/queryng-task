{% extends 'base.html.twig' %}

{% block title %}Main page{% endblock %}

{% block navbar %}
    {{ include('main/_navbar.html.twig') }}
{% endblock %}

{% block container %}
    <pre>
        <code>
            sudo sysctl -w vm.max_map_count=262144
            php bin/console doctrine:migrations:diff
            php bin/console doctrine:migrations:migrate
        </code>
    </pre>

    <div>
        <h1>Строка случайных чисел</h1>
        {{ simpleCacheExample }}

        <div class="row mt-5">
            <h1 class="h1 mt-5 mb-5">render_esi</h1>
            <p>
                render_esi позволяет не кешировать страницу целиком, позволяет иметь динамические секции
            </p>
            {{ render_esi(path('datenow')) }}
        </div>
    </div>
{% endblock %}
